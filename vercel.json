{
    "version": 2,
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/server/server.js"
        }
    ],
    "functions": {
        "server/server.js": {
            "runtime": "nodejs20.x",
            "includeFiles": "server/prisma/**"
        }
    }
}